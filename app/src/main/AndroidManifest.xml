<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asmt2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Asmt2">

        <!-- Main Activity Declaration -->
        <activity android:name=".MainActivity"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add your other activities here -->
        <activity android:name=".AddAddressActivity" android:exported="true"/>
        <activity android:name=".EditAddressActivity" android:exported="true" />
        <activity android:name=".AllAddressesActivity" android:exported="true" />

    </application>

</manifest>
